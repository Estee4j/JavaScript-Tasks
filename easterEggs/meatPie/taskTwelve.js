const prompt = require("prompt-sync")();
 let first_number  = Number( prompt("Enter  first_number: "))
 let second_number  = Number( prompt("Enter second_number: "))

console.log(first_number, second_number)
 
