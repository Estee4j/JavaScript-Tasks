const prompt = require("prompt-sync")();
let number = Number(prompt ("Enter your number "));
   if (number % 2 == 0){
    console.log(`${number} your number is an even number`)
    }else if (number / 2 != 0){
        console.log(`${number} your number is an even number`)
    }
